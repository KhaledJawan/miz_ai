org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template.
#
# Left false (not true) even though mobile_scanner 7.4.0 already supports
# built-in Kotlin: flipping it also forces every other plugin's Gradle
# script down the built-in-Kotlin path, and app_links (a transitive
# dependency, still on legacy KGP as of 7.2.1) hard-fails the build under
# AGP 9 when this is true ("the 'org.jetbrains.kotlin.android' plugin is no
# longer required... since AGP 9.0"). Revisit once app_links ships a
# built-in-Kotlin-compatible release -- see CLAUDE.md decision log if this
# needs documenting further.
android.builtInKotlin=false
