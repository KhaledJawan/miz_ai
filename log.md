# Agent Change Log

## Rules

This file is the append-only record of changes made by agents working in this repository.

1. Every agent that modifies repository files must add one summary entry before finishing its task.
2. Before adding an entry, read this file again and find the highest existing entry number.
3. The next entry number is the highest number plus one. If there are no entries, start at `1000`.
4. Use exactly this single-line format: `NUMBER - AgentName [Concise summary of the completed changes]`.
5. Keep the summary factual and concise. Mention the main implementation, documentation, tests, or verification completed.
6. Never include secrets, credentials, tokens, private user data, or sensitive environment values.
7. Never edit, reorder, renumber, or delete an existing entry. If an entry needs correction, append a new correction entry.
8. Re-read the latest entry immediately before appending so concurrent agents do not intentionally reuse a number. If the intended number is already present, increment again.
9. Add the log entry only after the work is complete, so it describes the final result accurately.

## Entries

1000 - Codex [Created the append-only agent change log and registered its required workflow in the repository instructions and README.]
1001 - Codex [Fixed the Home composer to animate above software-keyboard insets and added a regression test confirming the input remains visible; analyze and all 15 tests pass.]
1002 - Claude [Sourced 7 local restaurant photos (Wikimedia Commons) into assets/images/restaurants/, added Restaurant.imageAsset and wired MockRestaurantRepository; removed the circular "M" avatar mark from HomeHeader, keeping the "Miz" wordmark only. Verified against the current Soft Orbit design (core/widgets, DesignGD.md); flutter analyze clean, all 15 tests pass.]
1003 - Codex [Refined Soft Orbit to a black, white, neutral-gray, and Miz-red interface; removed colorful tokens, multicolor gradients, and tinted shadows; rebuilt the orb, quick actions, offers, backdrop, onboarding accents, and profile treatment; synchronized design docs and ADR-011; analyze and all 15 tests pass.]
1004 - Codex [Fixed clipped shadows around the four Home quick-action cards by disabling grid viewport clipping and added a regression assertion; analyze and all 15 tests pass.]
1005 - Codex [Corrected the remaining Home quick-action shadow clipping by moving elevation outside the rounded Material, increasing grid spacing, and replacing unsupported rounded icon glyphs with supported icons and a text arrow; analyze and all 15 tests pass.]
1006 - Codex [Compacted Home by moving Today's Offers into the removed greeting-card position, deleting Popular Cravings and meal-period chip sections, changing the composer prompt to “What do you want to eat?”, and synchronizing product/design docs; analyze and all 15 tests pass.]
1007 - Codex [Removed the Nearby restaurant rail and its loading state from Home, updated tests to prevent the section returning, and synchronized product/design documentation so nearby browsing lives in Discovery; analyze and all 15 tests pass.]
1008 - Codex [Added typed English, Farsi, and German localization with a working language picker, automatic RTL/LTR layouts, localized UI and semantics, platform locale metadata, extension documentation, and 20 passing tests; analyze and Android debug build pass.]
1009 - Codex [Persisted the selected locale through a tested LanguageStore boundary, added compact German and Farsi Home layout coverage, and reverified analyze, all 21 tests, platform metadata, and the Android debug build.]
1010 - Claude [Replaced the placeholder app-intro onboarding with a full local-only, deterministic (no AI) Food Preference Profile: a live Drift/SQLite database (core/database/, ~19 tables, seeded allergens/intolerances/food rules/ingredients/cuisines/flavors/~24 sample foods with bundled Wikimedia photos), an 11-screen adaptive onboarding flow (features/food_profile/presentation/onboarding/, diet-adaptive protein questions, inline severe-allergy confirmation, per-screen persistence/resume, skip-never-reopens), a permanent editable Food Profile section in Settings reusing the same step widgets with immediate-write persistence, and deterministic FoodEligibilityService/ProfileCompletenessService/InteractionTracker/BehavioralInferenceService (allergy/intolerance/restriction data kept on independent axes from preference/dislike throughout). Removed the superseded features/onboarding/. Added ADR-013/014/015/016 and a new "Local database (Drift)" section in docs/DATABASE.md. flutter analyze clean; 54 tests pass (17 new); Android debug build passes; full onboarding + Settings edit flow visually verified on macOS.]
1011 - Claude [Fixed a real-device hang introduced by entry 1010: sqlite3_flutter_libs was pinned to ^0.6.0+eol, an empty stub package with no bundled native SQLite library, so opening the local database in bootstrap.dart before runApp never resolved and the app was stuck forever on the native splash screen on Android (confirmed via adb logcat: the engine endlessly retried its first frame with no Dart-side exception). Repinned to ^0.5.42, the last version that actually bundles the native library. Added ADR-017 documenting the root cause and fix. Verified with a full uninstall/reinstall on a physical Android device (API 36): app now reaches the Welcome screen. flutter analyze clean; all 54 tests still pass.]
1012 - Codex [Implemented the Miz Spatial Glass redesign across focused Home, city selection, spatial conversation and navigation, Drift-backed unified bookmarks, combined profile/settings, and a single three-mode camera, QR, and menu-scan architecture with honest unavailable states; localized English, Farsi, and German, migrated Drift to schema v2, synchronized product and engineering docs, and verified clean analysis, 68 tests, light/dark visual references, macOS debug, and Android debug/release builds.]
1013 - Codex [Refined only the Spatial Home presentation with clearer lower-blur food imagery, more opaque borderless city/input/action surfaces, layered neutral iOS-style shadows, a borderless shadowed send control, updated light/dark visual references, and regression coverage; analyze and all 68 tests pass.]
1014 - Codex [Improved Home control readability by reducing prominent-surface backdrop blur to 3, raising light/dark opacity to 98/96 percent, and increasing rotating-prompt contrast without changing secondary-page styling; refreshed visual references, analyze is clean, and all 68 tests pass.]
1015 - Codex [Corrected the Home clarity treatment by removing prominent-control backdrop blur entirely, restoring controlled 70/72 percent translucency, sharpening animated food layers from blur 12 to 4, and reducing background dimming so imagery, text, and icons remain visible; refreshed light/dark visual references, analyze is clean, and all 68 tests pass.]
1016 - Codex [Made every prominent Home control a fully opaque pure-white, zero-blur surface in light and dark modes, removed the inherited glass gradient, and forced readable black foreground content while preserving borderless iOS-style shadows and leaving secondary pages unchanged; refreshed visual references, analyze is clean, and all 68 tests pass.]
1017 - Codex [Replaced only the Home background with original theme-aware Miz culinary-AI artwork combining floating ingredients, restrained magical red energy, neural connections, and refracted droplets; compressed and bundled separate light/dark local assets, added slow lifecycle-aware drift without blur, refreshed visual references, and verified clean analysis, all 68 tests, and an Android debug build.]
1018 - Codex [Applied a controlled 20-percent blur to only the theme-aware Home artwork using sigma 5.6 while keeping all white controls completely sharp; refreshed light/dark visual references, analyze is clean, and all 68 tests pass.]
1019 - Codex [Extended the approved culinary-AI visual system across conversation, city, bookmarks, camera, profile/settings, Food Profile, sheets, results, and secondary controls with static theme-aware 20-percent-blurred artwork, solid-white zero-blur borderless surfaces, black foreground content, and neutral iOS-style shadows; synchronized design docs, added surface and City/Camera visual regression coverage, and verified clean formatting, analysis, all 71 tests, visual references, and an Android debug build.]
1020 - Claude [Swapped the three Home circle-button icons for clearer alternatives: camera_alt_rounded to photo_camera_rounded (removes the dated flash-bump silhouette), bookmark_rounded to bookmarks_rounded (a stacked glyph that better represents the multi-category saved-items collection), and person_rounded to account_circle_rounded (adds a framing ring instead of a flat silhouette blob). Verified visually on macOS; flutter analyze clean; all 71 tests pass.]
1021 - Claude [Refined the Home camera icon from photo_camera_rounded to camera_rounded (a bolder aperture glyph) so its visual weight matches the solid bookmarks_rounded and account_circle_rounded icons beside it, confirmed against docs/DESIGN.md's "exactly three unlabeled circular actions" Home contract (no labels added). Regenerated lib/l10n (generated AppLocalizations files were missing from disk, unrelated to this change, causing a 32-error analyze; not a code regression). Verified visually on macOS; flutter analyze clean; all 71 tests pass.]
1022 - Codex [Integrated the supplied live Supabase project into Miz's existing bootstrap with validated HTTPS build-time configuration, current publishable-key initialization, a git-ignored local environment file and safe committed template, four configuration tests, and synchronized architecture, database, deployment, security, README, and changelog documentation; verified the endpoint/key, clean analysis, all 75 tests, and an environment-configured Android debug build without adding the unrelated Todos demo or unreviewed database access.]
1023 - Codex [Prepared repeatable new-project Supabase onboarding with a key-safe configuration verifier and a dedicated setup guide covering the exact Project URL/publishable-key values, replacement commands, RLS requirements, prohibited server secrets, and Auth/Storage/Realtime/CI boundaries; linked the guide from README, Deployment, Security, and Changelog, validated the current configuration, regenerated missing localization outputs, and verified clean analysis and all 75 tests.]
1024 - Codex [Replaced the unavailable city-location adapter with real foreground approximate device lookup through geolocator, local nearest-supported-city matching with no coordinate persistence or upload, Android/iOS/macOS permission configuration, localized unavailable guidance, injectable gateway tests, and synchronized design, architecture, privacy, roadmap, decision, testing, and changelog documentation; verified clean analysis, all 79 tests, Android/macOS debug builds, and installation with the expected runtime permission on the connected physical Android device.]
1025 - Claude [Replaced the honest-unavailable chat stub with a real, secure Gemini function-calling backend: a new supabase/functions/miz-ai/ Deno Edge Function using the Gemini Interactions API (model configurable via GEMINI_MODEL, defaulting to gemini-3.6-flash), a bounded (max 3 rounds) function-calling loop, and two backend-validated tools (search_nearby_places via Google Places API New, get_user_food_profile from a server-revalidated client-supplied summary of the local, not-yet-synced Food Profile). Gemini only ever proposes a tool name and arguments; the server allowlists, validates, and executes every call itself, and the search_nearby_places schema has no latitude/longitude field so trusted location is always server-injected, never model-supplied. Neither GEMINI_API_KEY nor GOOGLE_PLACES_API_KEY, nor any raw provider error/stack trace, ever reaches Flutter or a client response — all failures map to one of 11 stable error codes. Added Flutter-side MizAiService (Supabase FunctionsClient-backed), widened ConversationRequest/ConversationReply models, a foodProfileContext-building helper, an additive city-coordinates lookup and permission-safe transient position reader (no new permission prompts, no persistence), and conversation_controller/conversation_page support for place-result cards and a location-required flow reusing the existing city picker. Added 62 Deno unit tests (fetch stubbed, zero live network calls) and 32 new Flutter tests; wrote docs/EDGE_FUNCTIONS.md and ADR-021, updated docs/API.md, docs/SECURITY.md, docs/DEPLOYMENT.md. Verified: deno fmt/check/test clean, flutter analyze clean, all 111 Flutter tests pass, Android debug build passes. No live end-to-end call was made against real Gemini/Places (only the user holds those secrets); manual curl/in-app test steps are documented in docs/EDGE_FUNCTIONS.md for the user to run.]
1026 - Codex [Prepared `miz-ai` for secure deployment with new Supabase publishable keys by disabling only the incompatible legacy gateway JWT check, validating the `apikey` header against platform-injected named publishable keys before request parsing or paid provider calls, retaining local/legacy low-privilege fallbacks, adding four authorization tests, and updating Edge Function, security, and changelog documentation; verified Deno formatting/type checks, all 66 function tests, clean Flutter analysis, and confirmed both required remote secret names exist without exposing their values.]
1027 - Claude [Fixed a silent-failure bug preventing all outbound network calls (including the new miz-ai chat backend) from macOS and Android builds: macos/Runner/{DebugProfile,Release}.entitlements had App Sandbox enabled with com.apple.security.network.server but no com.apple.security.network.client, and android/app/src/main/AndroidManifest.xml declared no android.permission.INTERNET at all — both platforms silently blocked every outgoing HTTPS request (Supabase REST/Functions, not just chat) with the OS sandbox/permission system rather than a catchable app-level error, which is why no request ever reached the Edge Function logs and the chat UI showed no error. Added the missing entitlement to both macOS entitlements files and the missing permission to AndroidManifest.xml. Also added .vscode/launch.json (debug/profile/release configs passing --dart-define-from-file=.env.json) so the IDE Run/Debug button initializes Supabase instead of silently falling back to the offline stub. Verified: entitlements/manifest XML well-formed (plutil -lint, XML parse).]
1028 - Codex [Hardened and deployed miz-ai with layered deadlines and selective retries, strict two-tool schemas and recoverable model/tool failures, explicit structured location handling, safe request observability, grounded typed-only Places results, structured Flutter error states in three languages, synchronized architecture/security/design docs, 78 passing Deno tests, clean analysis, all 122 Flutter tests, live Gemini/Places smoke tests, and installation on the connected Android device.]
1029 - Claude [Closed the last gap in ConversationController's exception handling reported after a live smoke test: every catch branch (ConversationUnavailableException, ConversationAiException, and the generic catch-all) previously discarded the caught exception with no logging at all, so any bug in MizAiService's response parsing — not just a real backend error — would silently collapse into a generic error card with no trace of what actually happened, which is what made a confirmed-successful (HTTP 200, tools succeeded) backend response indistinguishable from a real failure. Added lib/core/logging/app_logger.dart (the "logging wrapper in core/" CLAUDE.md §4 already mandated but that didn't exist yet), logged every caught exception with its stack trace via dart:developer.log, and added debugErrorCode/debugErrorDetail fields to ConversationState (cleared on every new request/success) surfaced only in conversation_page.dart's error card body under kDebugMode — never in a release build. Added 3 Flutter unit tests covering an unclassified exception, a ConversationAiException, and clearing on subsequent success. Verified: flutter analyze clean, all 125 tests pass (up from 122).]
1030 - Claude [Root-caused the "CONVERSATION_UNAVAILABLE: Instance of ConversationUnavailableException" debug error reported after entry 1029's logging landed: confirmed by direct inspection that this app has no local conversation/message persistence layer at all (no DAOs, no conversation table, no activeConversationProvider — the chat is intentionally in-memory), so the reported bug could only be one of MizAiService.respond()'s two ConversationUnavailableException throw sites, not a persistence/session/Riverpod-race issue. Found that `if (data is! Map) throw ConversationUnavailableException()` fires whenever FunctionsClient decodes a 2xx body as a raw String instead of a Map (i.e. the client observed a non-application/json Content-Type even though the Edge Function's own logs show it returned valid JSON — most likely a proxy/gateway header rewrite), which discarded a real backend answer with no diagnostic trail. Added a String-body JSON-decode fallback in MizAiService so a stringified JSON body is recovered instead of discarded, and a new AiResponseFormatException (with the received type and a body preview) for genuinely unparseable bodies, separating "backend unreachable" from "backend answered but the client couldn't parse it." Gave both ConversationUnavailableException and AiResponseFormatException real toString() implementations (including the underlying SocketException detail) so the debug card never again shows "Instance of ...". Files: lib/features/conversation/domain/conversation_models.dart, conversation_service.dart, lib/features/conversation/data/miz_ai_service.dart, unavailable_conversation_service.dart. Did not build the local conversation-persistence/session database, DAOs, or ensureActiveConversation locking described in the follow-up request — no such layer exists in this codebase today and adding one is a separate, deliberate feature decision, not part of this bug fix; flagged to the user. Added 6 tests (unparseable string body, non-map/non-string body, string-JSON recovery, SocketException reason, controller-level AiResponseFormatException handling, toString regression). Verified: flutter analyze clean, all 130 tests pass (up from 125), Android debug build (--dart-define-from-file=.env.json) succeeds.]
1031 - Claude [Found and fixed the actual live "send hi, show restaurants near me" bug end-to-end by driving the real app on the connected Android device (adb) against the deployed miz-ai project, rather than static analysis alone: switched lib/core/logging/app_logger.dart to also debugPrint in debug builds (dart:developer.log alone never reaches the flutter run console, only DevTools), added debug-only request-shape logging in miz_ai_service.dart, and used both to capture the exact failing request live. Root cause: the requiresLocation short-circuit is a normal 200 success response with message:"" (not a thrown exception), so ConversationController appended an empty-text assistant message to state.messages; on the user's next action (selecting a city, which auto-retries the same prompt) that empty-text message survived into the request's history array, and supabase/functions/miz-ai/request_schema.ts's validateHistory rejected any history turn with text.length === 0, producing INVALID_REQUEST — discarding a fully successful retry. Fixed on both sides: request_schema.ts now only requires non-empty text for role "user" (an assistant turn can legitimately have no text — gemini_loop.ts itself produces that for requiresLocation and card-only place results) and redeployed miz-ai; conversation_controller.dart now only appends a message when the reply has text or places, eliminating both the invalid history entry and a cosmetic empty "ghost bubble" that was rendering in the UI. Added 2 Deno tests and 2 Flutter tests (plus strengthened an existing one) covering the exact regression. Verified live end-to-end on the physical device after a clean uninstall/reinstall: "hi show me restaurants near me" -> location-required -> select Berlin -> auto-retry now returns real Google Places results (Berlin TV Tower, Holzmarkt 25, Café am Neuen See, RAW-Gelände, Park Inn) with zero errors in the debug log. Also verified: deno check/test (80/80), flutter analyze clean, all 132 Flutter tests pass (up from 130).]
1032 - Codex [Redesigned the conversation experience with clearly distinct solid-black user bubbles and white Miz replies with a red AI marker; removed every message copy icon and the page-level X; made send explicitly dismiss the keyboard and scroll to the newest state; replaced destructive reset behavior with History and New Chat actions; added a localized English/Farsi/German offline History page with review/delete; introduced Drift schema v3 conversation archives with typed message/place JSON round-tripping and privacy-safe exclusions; synchronized DesignGD, product, architecture, database, API, security, decision, README, and changelog docs; verified clean analysis, all 138 Flutter tests, diff hygiene, and an environment-configured Android debug APK build.]
1033 - Codex [Activated secure menu-photo explanation from the native camera or gallery with one-to-four-page review, reorder/delete, explicit upload consent, localized English/German/Farsi result and recovery states, typed dish/price/dietary/allergen output, platform permissions, and temporary-file safety; implemented, tested, deployed, and live-smoke-verified the bounded `analyze-menu` Supabase/Gemini Edge Function without image persistence or content logging; synchronized product, design, architecture, API, security, deployment, roadmap, testing, decision, and changelog documentation; verified clean analysis, all 141 Flutter tests, 10 function tests, diff hygiene, and an installed Android debug build.]
1034 - Codex [Traced the reported production menu failure by request ID to successful Gemini responses rejected at `semantic` and `outer_json` parsing; replaced the brittle double-encoded analysis string with shallow structured fields and bounded delimiter-safe dish records, added content-safe wrapper recovery, discarded only malformed dishes instead of the complete menu, constrained response length, updated API/security/changelog documentation, expanded the function suite to 13 passing tests, redeployed `analyze-menu`, and verified the live endpoint returns a successful typed response.]
1035 - Codex [Activated Food mode with explicit camera/gallery review, localized typed candidate results, confidence and safety states backed by a new bounded `analyze-food` Supabase/Gemini Edge Function; replaced the Miz QR placeholder with a real `mobile_scanner` live decoder, duplicate suppression, strict local Miz payload validation, and accurate trusted-verification errors; fixed capability failures leaking across camera modes, updated Android/iOS/macOS permissions and camera entitlements, synchronized product/design/architecture/API/security/deployment/testing/roadmap/decision/changelog documentation, deployed and live-tested food recognition, and verified clean analysis, 146 Flutter tests, 100 Edge Function tests, an Android debug build with camera permission, and successful installation/runtime launch on the connected device.]
1036 - Codex [Audited both Supabase projects and source repositories, extended Mizzz's existing catalog with additive phases 56–61 for controlled observations, evidence, proposals, audit/versioning, strict RLS, hybrid search, embeddings, licensed images, structured imports, safe menu matching and versioned Miz AI read RPCs; added Phase-62 SQL tests and synchronized cross-product documentation; disposable PostgreSQL migration and regression verification passes, with no production deployment.]
1037 - Claude [Mizzz repo: deployed entry 1036's phases 56-61 to production (user ran the final `supabase db push --linked` in their own terminal after a Docker-clone dry run against a real `supabase db dump --linked` schema clone found and fixed a real regression: phases 58-59 tightened `search_items`/RLS to require `verification_status='verified'`, not just `status='approved'`, breaking phase49's existing test — fixed the test and documented the operational implication in docs/CENTRAL_FOOD_CATALOG.md). Reconciled 19 pre-existing remote migration-history entries with no local file equivalents via `supabase migration repair --status reverted` (bookkeeping-only, confirmed no schema/data effect). Curated and validated 200 real German dishes/drinks/snacks/desserts/staples (ai-curated-general-knowledge, non-fabricated, human-review-required) matching the catalog import schema; found and fixed a real bug in the deployed `approve_food_proposal` function that silently dropped cuisine/halal/vegan/vegetarian/alcohol/etc. fields when approving a `create_food` proposal; built and deployed phase63 (non-login system identity for import attribution, Germany/German country+cuisine seed, the `approve_food_proposal` fix, and the 200-item import run via the real `create_food_import_batch`→`apply_food_import_batch` pipeline) — validated twice end-to-end in a disposable Postgres clone (200/200 valid, then 200/200 test-approved with 0 failures) before the user deployed it. The 200 dishes remain genuinely `pending` `create_food` proposals: confirmed by code inspection that `create_food` is structurally excluded from `evaluate_low_risk_food_proposal`'s auto-approval eligibility, so no bulk-approval shortcut was built — real human review is still required before they're searchable/live.]
1038 - Claude [Miz AI repo: replaced analyze-menu's single Gemini-explains-everything call with a 4-stage low-token pipeline per the user's architecture spec. Stage 1 (vision_parser.ts) is now a JSON-only Gemini extraction with no dietary/allergen guessing. Stage 2 (mizzz_catalog_client.ts, fuzzy_match.ts, safety_classifier.ts, stage2_matcher.ts) is a zero-LLM-call deterministic pipeline: fuzzy-matches each extracted dish against Mizzz's Central Food Catalog v2 read RPCs (food_catalog_v1_search/detail, anon-key, new MIZZZ_SUPABASE_URL/MIZZZ_SUPABASE_ANON_KEY secrets — not yet provisioned), classifies Safe/Warning/Restricted against the user's Food Profile (halal/pork/alcohol/vegan/vegetarian rules, allergies honestly flagged as unverifiable since the catalog has no allergen fields), and computes a relative price-value indicator; bounded to 40 matched items with 5-way concurrency and per-item fault isolation so one failed lookup never fails the scan. Stage 3 is the resulting typed MenuAnalysisResult (no LLM text). Stage 4 reuses the existing miz-ai function for a lightweight follow-up chat: a new `menuContext` field (server-validated, history capped to 5 turns) selects a minimal no-tools system prompt (buildMenuFollowUpSystemInstruction) that must never contradict the already-computed classifications. Rewrote the Flutter camera feature end-to-end to match (camera_models.dart, camera_services.dart, supabase_camera_analysis_service.dart, camera_workflow_controller.dart, menu_camera_experience.dart with Safe/Warning/Restricted + price badges and an "Ask Miz about this menu" action), added ConversationLaunchArgs/ChatLaunchArgs so a chat can carry a menuContext summary for its whole lifetime, and added ~30 new localized strings (en/de/fa). Documented the new secrets and the private `food-images` Storage bucket gap (images not yet renderable) in docs/EDGE_FUNCTIONS.md/docs/SECURITY.md. Deliberately did not build a bulk-image URL scheme or fabricate pairing/ingredient data the catalog doesn't expose. Verified: deno fmt/check clean, 137 Deno tests pass, flutter analyze clean, all 146 Flutter tests pass. No live end-to-end run yet — blocked on the Mizzz secrets being provisioned and on the 200 seeded dishes (entry 1037) still awaiting human review/approval.]
1039 - Claude [Miz AI repo: fixed a real, live production bug the user reported ("menu explanation doesn't work, always says menu was not clear enough") and shipped the user's requested camera UX simplification. Root cause: entry 1038's Stage 1 vision_parser.ts encoded extracted dishes as a hand-rolled `category|||name<TAB>price` delimited string inside a JSON schema array — never validated against a real Gemini response, and evidently failing to parse on essentially every real photo in production. Replaced with a plain nested JSON schema (categories: [{name, items: [{name, price}]}]), which structured-output schemas already support natively; rewrote parseVisionOutput and vision_parser_test.ts to match, redeployed analyze-menu, and reconfirmed the user had (separately, correctly) added MIZZZ_SUPABASE_URL/MIZZZ_SUPABASE_ANON_KEY as Edge Function secrets on the Miz AI project (verified read-only via `supabase secrets list`). Then removed the manual Food/Miz QR/Menu mode picker entirely per explicit user request ("a photo must be understood automatically"): built a new classify-capture Edge Function (its own minimal ~64-output-token Gemini call answering only menu/single_dish/unrecognized, never dietary/allergen/price content) plus 12 Deno tests; deployed it. Rebuilt the Flutter camera feature into one unified live screen: a real mobile_scanner preview watches continuously for a Miz QR code (still decoded on-device only) while Take photo/Choose photo are always available; a captured photo is reviewed once (one photo, one explicit Analyze consent tap) then classified and routed automatically — a single dish calls analyze-food immediately with no further tap, a menu enters the existing multi-page review unchanged, and an unrecognized photo shows an honest fallback. CameraMode became nullable; deleted the now-dead MizCameraModeSelector, MizQrScannerExperience, and FoodCaptureExperience's preview UI; added UnifiedScanExperience and CapturePreviewExperience; added/removed l10n keys accordingly (en/de/fa); added ADR-026 and updated docs/DESIGN.md, docs/CHANGELOG.md, docs/EDGE_FUNCTIONS.md. Found and fixed a real widget bug in the process (PositionedDirectional nested inside IgnorePointer instead of the other way around — Positioned must be a direct Stack child) and a real overflow bug (two capture buttons side-by-side didn't fit at 390px width; reverted to the app's existing stacked full-width button pattern). Regenerated the Camera golden reference and taught the affected widget tests to use a small bounded manual pump() loop instead of pumpAndSettle(), since a real MobileScanner preview is now mounted on the very first frame and its continuously-running platform channel never lets pumpAndSettle() converge. Verified: deno fmt/check clean, 157 Edge Function tests pass, flutter analyze clean, all 152 Flutter tests pass, classify-capture and the fixed analyze-menu are both deployed to the linked Supabase project.]
1040 - Claude [Miz AI repo: found and fixed the actual live cause of entry 1039's "Miz could not analyze this photo" report by reproducing it directly against the deployed backend — curled a real L'Osteria menu photo the user shared straight at classify-capture and got 503 AI_UNAVAILABLE. Root cause: both classify-capture (max_output_tokens: 64) and analyze-menu's vision_parser.ts Stage 1 (max_output_tokens: 2048) had output-token budgets too small for the reasoning-capable Gemini model — internal "thinking" tokens count against the same budget, so on a real (denser, non-trivial) photo the visible JSON answer got truncated to nothing before it was ever emitted, which the client code correctly but unhelpfully surfaces as "no output" -> AI_UNAVAILABLE. Raised classify-capture to 512 tokens and analyze-menu's Stage 1 to 8192 tokens (analyze-food's 1024 and miz-ai's 1024 were left unchanged — no observed failure there, and food/chat responses are inherently much shorter). Redeployed both functions and reverified live against the exact same photo: classify-capture now correctly returns {"kind":"menu"}, and analyze-menu correctly extracts all 27 dishes across all 5 categories (Antipasti/Pasta-Verde/Pasta-Carne/Pasta-Mare/Dolci) with correct prices, detects de/EUR, and one dish (Tiramisù) already matched the Mizzz catalog as safe. Verified: deno fmt/check clean, all 157 Edge Function tests still pass, both functions deployed.]
1041 - Claude [Shipped three user-requested changes to the menu scan result UI/pipeline. (1) Price display: replaced the "Good value"/"Above average price" text badges with a colored $ mark next to the price itself (green=good, amber=high, red=very_high); narrowed safety_classifier.ts's PriceValueIndicator from 4 states (below_average/average/above_average/unknown) to a clearer 3-tier good/high/very_high/unknown, added two new AppColors tokens (priceGood, priceElevated; red reuses the existing error token) as a deliberate, documented exception to the app's otherwise-monochrome palette. (2) Removed guest-facing wording that exposed backend internals: dropped the "Not in Miz's food database yet" badge (an unmatched dish now shows no badge at all, just name+price) and the "pairing suggestions... not available from the catalog" note from analyze-menu's response. (3) Built and deployed a new anon-safe Mizzz-side write path so unmatched dishes grow the catalog organically: `mizzz/supabase/phase64_menu_scan_candidate_submission.sql` adds `food_catalog_v1_propose_candidate(name, category, language, price)`, deduplicated by normalized name against both existing catalog.items and already-pending proposals, always creating a `pending` create_food proposal (never auto-approved, never a description/dietary claim — user explicitly confirmed via AskUserQuestion that descriptions should stay blank for their own review) attributed to a second non-login system identity distinct from phase63's import identity. Wired into stage2_matcher.ts: fires only when a lookup genuinely completed with no confident match (never on a transient search/network failure, which is not confirmation the dish is new), awaited but always caught so it can never fail or slow-fail the scan's response to the user. Found and fixed a real deployment-safety issue before it caused damage: local migration history showed phase63 (entry 1037's 200-dish import) was never recorded as applied in Mizzz's remote bookkeeping table, so `supabase db push --linked` would have silently re-run the entire import pipeline and created 200 duplicate pending proposals — caught via `supabase migration list --linked` before the user pushed, fixed with a bookkeeping-only `supabase migration repair --status applied` (confirmed via re-running list afterward: phase63 and phase64 both show matching local/remote). Validated phase64 end-to-end in a disposable Postgres clone as the `anon` role specifically (dedup by exact and normalized-whitespace name, already_exists vs duplicate_pending vs created paths, input validation, confirmed anon cannot read catalog.food_change_proposals directly, and a full round-trip through the existing approve_food_proposal correctly promoting an approved proposal into a real verified catalog.items row) before handing the user the deploy commands. Re-verified live end-to-end after both sides deployed: re-ran analyze-menu against the same real L'Osteria menu photo from entry 1040 — correct 3-tier price colors across all 5 categories, empty notes array (no internal-detail leak), unmatched dishes showing no badge, request completing successfully with the new propose-candidate calls wired in. Documented in mizzz/docs/CENTRAL_FOOD_CATALOG.md (new Phase 64 section + phase delivery record row) and this repo's docs/EDGE_FUNCTIONS.md/docs/CHANGELOG.md. Verified: deno fmt/check clean, all 163 Edge Function tests pass (+6 new), flutter analyze clean, all 152 Flutter tests pass, both analyze-menu (Miz AI) and phase64 (Mizzz) deployed to their respective linked production projects.]
